# Auth0 Authentication (REQUIRED for admin dashboard)
# See AUTH0-SETUP.md for setup instructions
PUBLIC_AUTH0_DOMAIN=your-tenant.auth0.com
PUBLIC_AUTH0_CLIENT_ID=your_auth0_client_id_here
# Namespace used for Auth0 custom claims (must match your Auth0 Action configuration)
PUBLIC_AUTH0_CLAIM_NAMESPACE=https://cms.local

# GitHub Repository Configuration (REQUIRED)
# The repository where content will be committed
PUBLIC_GITHUB_OWNER=your_github_username
PUBLIC_GITHUB_REPO=your-repo-name

# Site Identity
# Used as fallback meta description when a page does not define its own
PUBLIC_SITE_NAME=Your Site Name

# NOTE: GitHub Personal Access Token is now stored in Auth0 Action secrets
# See AUTH0-GITHUB-SETUP.md for configuration instructions

# Git Branch (auto-set by set-branch.js in dev mode)
# In dev: uses your current branch
# In production: always uses 'main'
# PUBLIC_GIT_BRANCH=main

# Static.app Deployment
STATIC_APP_API_KEY=your_static_app_api_key_here

# Optional: Analytics & Performance
# GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX
# PLAUSIBLE_DOMAIN=yourdomain.com
